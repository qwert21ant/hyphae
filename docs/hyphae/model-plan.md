# Hyphae model plan — hyphae

Generated by the building-architecture-models skill. Working/resume file.

## Drift (docs vs reality)
- README dev instructions say `pnpm --filter @hyphae/server dev` / `@hyphae/web dev`; root `package.json` `dev` script globs only `./apps/*`. → `pnpm-workspace.yaml` actually includes BOTH `packages/*` and `apps/*`. The `@hyphae/schema` package lives under `packages/`, so the root `dev` script (apps-only) intentionally does not start it (it's a library, not a long-running process). Not a real defect — recorded for completeness.
- README path conventions imply package names map 1:1 to folder names (`@hyphae/server` → `server`, etc.). → Confirmed: `@hyphae/schema`=`packages/schema`, `@hyphae/server`=`apps/server`, `@hyphae/web`=`apps/web`. No drift.
- Otherwise: none found. The existing Hyphae model already matches the verified repo layout exactly (3 packages → 3 Containers).

## Container map
| Container (package) | Path | Technology | Archetype | Drill? |
|---------------------|------|------------|-----------|--------|
| @hyphae/schema | packages/schema | TypeScript, Zod | library (public export surface in src/index.ts) | no — already drilled (validate, context Components exist) |
| @hyphae/server | apps/server | TypeScript, Hono, Node, MCP SDK | web service (routes + MCP tools) | no — already drilled (ModelStore, routes, mcp tools Components exist) |
| @hyphae/web | apps/web | TypeScript, Vite, React, Zustand, React Flow | frontend / UI (store, canvas, side panel) | no — already drilled (store, Canvas, SidePanel Components exist) |

## Progress
- [x] Phase 1 — System + Containers written (pre-existing; all create-or-skip → skipped, no duplicates created)
- [ ] GATE 1 approved
- [ ] Phase 2 — components per drilled container:
  - (none pending — all three containers are already populated with Components from a prior run)
- [ ] GATE 2 approved
- [ ] Phase 3 — connections + amendments + external systems applied
